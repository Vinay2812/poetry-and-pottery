
npm run codegen

if ! git diff --quiet src/graphql/generated/; then
  echo "Error: src/graphql/generated/ is out of date."
  echo "Please commit the updated generated files before pushing."
  exit 1
fi

npm run lint
npm run prettier